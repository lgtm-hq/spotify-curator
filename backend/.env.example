# Spotify (from developer dashboard)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=https://localhost:8000/auth/callback

DATABASE_URL=sqlite:///./spotify_curator.db
FRONTEND_URL=http://localhost:5173
# App
# REQUIRED (>=32 chars). Generate a unique value per deployment:
#   openssl rand -hex 32
# Leave blank here on purpose — copy to backend/.env and fill it in.
SECRET_KEY=

# AI (provider-agnostic — see config.yaml)
ANTHROPIC_API_KEY=
CURSOR_API_KEY=
OPENAI_API_KEY=
