# Spotify Curator — Frontend

React + TypeScript + Vite SPA for the Spotify Curator backend.

## Development

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — Vite dev server
- `npm run build` — type-check and production build
- `npm run lint` — oxlint
